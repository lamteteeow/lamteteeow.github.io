# Z-score scoring + weight rebalance

## Changes

### fetch_data.py
1. Add `z_score_sub_score(value, series, direction, window_days=3652)` — computes rolling-window z-score, maps to [0,1] via `clamp(z/2, 0, 1)` for up, `clamp(-z/2, 0, 1)` for down
2. Replace all 7 `sub_score_linear` calls with `z_score_sub_score`
3. Update weights: YC 0.30, Sahm 0.20, LEI 0.20, Credit 0.15, Buffett 0.10, CAPE 0.03, Sentiment 0.02

### index.html
- Update methodology table weights

## Verification
- `uv run pytest tests/ -v` — 19 pass
- `uv run fetch_data.py` — runs without API key
- JSON has correct scoring keys
